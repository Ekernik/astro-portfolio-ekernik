---
const { title, techList } = Astro.props;
---

<div class='bg-egg p-4 flex flex-col gap-0 border-border shadow-sm'>
  <h3 class='font-bold text-sm mb-2'>{title}</h3>

  {techList.map((tech: string) => <p class='text-sm'>{tech}</p>)}
</div>
